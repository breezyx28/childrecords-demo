generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model PartnerVisitor {
  id       Int     @id @default(autoincrement())
  fullname String
  partner  String?
  email    String  @unique
  phone    String  @unique
}
