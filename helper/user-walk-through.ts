import { isAuthenticated } from "@/redux/ApiConfig";
import { setCookie } from "cookies-next";

export const SubscriptionWalkThrough = () => {
  // check if the user is authed
  if (isAuthenticated()) {
  }
};
